name: Weekly Firestore Backup (disabled - migrate to private storage)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  backup:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Backups must be stored in restricted storage, never committed to this public repository."
