{
  "name": "fallball-access-tests",
  "private": true,
  "scripts": {
    "test": "node --test tests/access-session.test.cjs",
    "test:rules": "firebase emulators:exec --project demo-fallball --only firestore \"node --test tests/firestore.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
